<p id="notice"><%= notice %></p>

<h1>Messages</h1>

<!-- <table>
  <thead>
    <tr>
      <th>Senderid</th>
      <th>Receiverid</th>
      <th>Message</th>
      <th>Date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @messages.each do |message| %>
      <tr>
        <td><%= message.senderId %></td>
        <td><%= message.receiverId %></td>
        <td><%= message.message %></td>
        <td><%= message.date %></td>
        <td><%= link_to 'Show', message %></td>
<!--         <td><%= link_to 'Edit', edit_message_path(message) %></td>
        <td><%= link_to 'Destroy', message, method: :delete, data: { confirm: 'Are you sure?' } %></td>
 --> 
<!--       </tr>
    <% end %>
  </tbody>
</table> -->




<div class="list-group">
    <% @inbox_messages.each do |inbox_message| %>
      <a href='/messages/<%= inbox_message.message.id %>' class="list-group-item">
        <h4 class="list-group-item-heading">New Message</h4>  
       From:   <%= inbox_message.sender.email %> 
  
      </a>
    <% end %>

</div>


<br>

<%= link_to 'New Message', new_message_path %>

